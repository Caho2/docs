---
title: "场景二：论文参考文献国家分析"
description: "上传论文 PDF，自动检查参考文献是否来自足够多的国家，几句话搭出完整的 Web 工具"
---

## 背景

很多期刊和基金申请要求论文的参考文献"至少 5 篇、来自 3 个及以上国家"。手动核查的流程极为繁琐：

1. 从 PDF 里把参考文献一条条复制出来
2. 逐篇去 Google Scholar 或数据库查找作者单位
3. 手动记录每位作者的国籍，汇总统计

一篇论文几十条参考文献，光查询就要花半小时以上，还容易漏查。

<Info>
  **这是一个"从零搭工具"的场景。** 不是让 Claude Code 帮你操作已有软件，而是直接用对话把一个完整的 Web 应用搭出来——前端页面、后端接口、OCR 识别、学术数据库查询，全部一次性完成。
</Info>

## 工具功能

最终搭出来的工具只需三步：

```text
上传论文 PDF  →  自动识别全部参考文献  →  查询每篇文献的作者来源国并汇总
```

<Frame>
  ![Image](/images/image-23.png)
</Frame>

<Frame>
  ![Image](/images/image-24.png)
</Frame>

结果页面会显示：

- 是否满足"≥5 篇、来自 ≥3 个国家"的要求
- 每条参考文献对应哪些国家
- 所有涉及国家的完整列表

<Frame>
  ![Image](/images/image-25.png)
</Frame>

## 实现原理

Claude Code 搭建的工具由三层组成：

```text
前端（HTML/Tailwind）
    ↓ 上传 PDF
后端（Python / FastAPI）
    ↓ ① OCR：调用 PaddleOCR API，将 PDF 转为 Markdown
      ② 解析：从 Markdown 中识别参考文献区域，逐条提取
      ③ 查询：调用 OpenAlex API，查询每篇文献的作者所属国家
    ↓ 返回 JSON 结果
前端渲染结果卡片
```

整个过程完全自动，用户只需上传 PDF，等待 30 秒到 2 分钟即可看到结果。

## 对话过程

### 第一轮：描述需求

```text
我需要构建一个论文参考文献国家来源分析工具，用于检测一篇论文的参考文献是否满足"作者来源不少于3个国家"的要求。
功能流程：

用户上传 PDF 论文
调用 PaddleOCR API（调用方式见 D:\PythonProject\Course\paddleOCR-VL-1.5.txt）提取 PDF 文本（markdown 格式）
从提取的文本中定位并解析参考文献部分，提取每条文献的标题
通过 OpenAlex Works API 用标题搜索每篇参考文献，获取 authorships 字段中的 countries 信息
若某篇文献的 countr



ies 为空，则进一步调用 OpenAlex Author API，通过 author id 查询 affiliations[].institution.country_code 获取国家信息
汇总所有参考文献的作者国家，判断是否满足：参考文献 ≥ 5 条，且作者来源国家 ≥ 3 个
前端展示每条参考文献的标题、作者、国家来源，以及最终的合规性判断结果

技术要求：

用 Python 构建后端，使用 uv 新建虚拟环境并管理依赖
前端使用 TailwindCSS 制作简洁 UI，支持 PDF 上传和结果展示
项目结构清晰，前后端分离

OpenAlex 数据结构参考：
Works API 返回的 authorships 示例：
json"authorships": [{
  "author": { "id": "https://openalex.org/A5087294524", "display_name": "R Core Team" },
  "institutions": [],
  "countries": []
}]
若 countries 为空，则调用 Author API，返回的 affiliations 示例：
json"affiliations": [{
  "institution": { "country_code": "JP", "display_name": "National Institutes of Natural Sciences" },
  "years": [2025, 2024]
}]
请先读取 D:\PythonProject\Course\paddleOCR-VL-1.5.txt 了解 PaddleOCR 的调用方式，再开始搭建项目。
```

Claude Code 直接生成了：

- `main.py`：FastAPI 后端，包含 OCR、参考文献解析、OpenAlex 查询全部逻辑
- `static/index.html`：带拖拽上传、进度条、结果展示的完整前端页面

### 第二轮：运行与调试

遇到 500 报错后，直接把报错信息发给 Claude Code：

```text
帮我检查一下为什么接口报错：
POST http://localhost:8000/api/analyze 500 (Internal Server Error)
```

Claude Code 同时读取了前后端代码，定位到两处问题：

- 后端缺少 `try/except`，异常以纯文本形式返回而非 JSON
- 前端未检查 `resp.ok` 就直接调用 `.json()`，导致解析失败

两处代码各修改几行，问题解决。

## 核心代码结构

<Tabs>
  <Tab title="后端接口">
    ```python
    @app.post("/api/analyze")
    async def analyze_pdf(file: UploadFile = File(...)):
        pdf_bytes = await file.read()
    
        # Step 1：OCR 识别 PDF
        try:
            markdown = await pdf_to_markdown(pdf_bytes)
        except Exception as e:
            return {"success": False, "error": f"OCR 识别失败：{e}"}
    
        # Step 2：提取参考文献条目
        refs = extract_references(markdown)
        if not refs:
            return {"success": False, "error": "未识别到参考文献"}
    
        # Step 3：并发查询 OpenAlex
        sem = asyncio.Semaphore(5)
        async with httpx.AsyncClient(timeout=30) as client:
            tasks = [analyze_one_reference(client, ref, i+1, sem)
                     for i, ref in enumerate(refs)]
            results = await asyncio.gather(*tasks)
    
        return {
            "success": True,
            "total_refs": len(refs),
            "all_countries": sorted(all_countries),
            "meets_requirement": len(all_countries) >= 3 and len(refs) >= 5,
            "references": results,
        }
    ```
  </Tab>
</Tabs>

## 可以进一步扩展的方向

工具搭好后，可以继续用对话扩展功能：

```text
帮我在结果页面加一个"导出 Excel"按钮，
把每条参考文献、对应国家、是否在 OpenAlex 找到，导出成表格。
```

```text
在现有参考文献国家来源分析工具的基础上，新增文献引用数据分析功能：

1.各文献总被引次数柱状图
- X 轴：参考文献编号或短标题
- Y 轴：总被引次数（cited_by_count）
按被引次数从高到低排序

2.年度被引趋势折线图（只展示被引次数前十的文献）
- X 轴：年份
- Y 轴：被引次数
-每条折线代表一篇参考文献
支持图例，可区分不同文献
```

```text
帮我加一个批量模式：支持同时上传多篇论文，
生成一份汇总表，每行对应一篇论文及其达标状态。
```

<Tip>
  **起点越具体，代码越准确。** 提需求时把"用什么 API""返回什么格式""达标条件是什么"一起说清楚，Claude Code 往往第一轮就能给出可运行的版本，不需要来回调整。
</Tip>